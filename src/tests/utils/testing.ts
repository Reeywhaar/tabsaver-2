import { act as _act, fireEvent as _fireEvent, render as _render, renderHook as _renderHook, waitFor as _waitFor } from '@testing-library/react'

export const act = _act
export const render = _render
export const renderHook = _renderHook
export const fireEvent = _fireEvent
export const waitFor = _waitFor
